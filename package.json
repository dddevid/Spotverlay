{
  "name": "spotverlay",
  "version": "2.0.0",
  "description": "Always-on-top music overlay for Windows, macOS and Linux — no API key, no DLL injection.",
  "scripts": {
    "tauri": "tauri",
    "dev": "tauri dev",
    "build": "tauri build"
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2"
  },
  "dependencies": {
    "@tauri-apps/api": "^2"
  }
}
